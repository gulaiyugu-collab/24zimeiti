# 项目024进度

- 更新时间：2026-08-29
- 当前阶段：手机端云端最小闭环与小范围朋友内测准备；安装包商业化方向保留为后续路线
- 当前状态：P2-02 旧活动入口已归档；P3-07 双体验模式与桌面研发基线已完成验收；用户最新决定将下一阶段改为手机端云端 MVP，小范围朋友内测只授予使用权，先验证需求、留存、失败场景和成本，再决定安装包与混合版投入
- 当前活动范围：收敛手机 MVP、受邀账号、HTTPS、最小隔离、云端任务状态、内测反馈和成本记录
- UI 方案状态：用户已授权修改前端；“创作陪跑 / 安心交付”正式作为同一产品的两种体验模式实施
- 权威任务清单：`G:\Workspace\Projects\项目024_自媒体通关搭档\feature_list.json`

## 2026-08-29 云端 Worker 可行性预检（进行中）

- 用户已确认执行手机云端 MVP 的下一步，并要求把过程实时同步到项目日志，防止上下文压缩或切换对话后失忆。
- 本轮目标：在不购买云 GPU、不上传凭据的前提下，确认现有 Worker 能否脱离桌面入口运行，并记录进入云端 Worker 实验的阻塞条件。
- 已验证：`工作文件\app\.venv\Scripts\python.exe -m app.acquisition_worker --help` 正常退出（exit code 0），独立 Worker CLI 入口存在。
- 已验证：`工作文件\app\Dockerfile` 只构建 FastAPI 主服务，监听 `0.0.0.0:8787`；没有云端任务领取、心跳、结果回传或独立 Worker 容器入口。
- 已验证：当前 `run.ps1` 仍只允许 `127.0.0.1`、`localhost`、`::1`，公网手机访问前必须先补访问控制。
- 阻塞：本机未找到 `docker` 命令，当前不能进行容器构建或容器内 Worker 验收；也没有获得云端账号、部署入口或付费 GPU 授权，因此本轮没有进行外部部署或付费调用。
- 当前结论：现有本地 Worker 可以作为云端 Worker 实验的处理内核，但尚未具备“云端任务拉取 → 租约/心跳 → 结果回传”的控制面；私有电脑方案仍会依赖电脑在线，不能宣称全天候可用。
- 已完成本原子步骤：新增 `工作文件\app\app\services\cloud_worker_protocol.py` 协议样机和 `tests\test_cloud_worker_protocol.py`，覆盖任务幂等、原子领取、Worker 租约/心跳、完成回传、失败重试和租约过期恢复；定向测试 6/6 通过，源码与测试 `py_compile` 通过。
- 原始环境问题：首次 `compileall` 尝试写入既有 `tests\__pycache__` 时返回 `WinError 5 拒绝访问`；未删除或覆盖缓存，拆分为源码/测试 `py_compile` 后均通过。
- 已完成本原子步骤：新增 `工作文件\app\app\services\cloud_worker_http.py` 和 `tests\test_cloud_worker_http.py`，用可替换 `CloudTaskBackend` 契约验证用户隔离、Worker 领取、心跳、完成/失败回传和 HTTP 错误边界；协议与 HTTP 合约合计 10/10 通过，四个文件 `py_compile` 通过。
- 精确下一步：申请一次明确授权的真实云端单任务对比（CPU 常驻或按需 GPU 二选一），记录启动/处理/回传耗时、失败率、模型成本和电脑离线时的用户体验；在授权前不购买资源、不上传密钥。

## 2026-08-29 手机端/电脑端 UI 同步完成

- 用户要求先同步手机端和电脑端 UI/交互，完成后再进入云端部署入口与账号；本轮只改活动前端和回归脚本，不改云端部署控制面。
- 首页移除此前暂停的粉色 Bento 内联主题和翻牌式补充资料交互，恢复与抖音运营/发布校准一致的浅灰画布、白色工作面、绿色强调和 6～10px 面板体系。
- 补充资料改为标准 `<details>` 展开区，保留 `#supplementDetails`、`#supplementNote` 和 Agent 打开入口；不改变字幕、音视频、商品事实、ASR 和地区字段语义。
- 移动端不再隐藏“本次工作总览”，只在 390px 改为单列；首页、抖音运营、发布校准统一静态资源版本号 `20260829-2`。
- 更新 `tests\browser_v03.cjs`：回归当前批准文案“交给系统处理 / 生成我的原创稿”，并新增补充资料展开与移动/桌面工作总览存在断言。
- 真实 8792 验收：首页桌面/390px 38/38、抖音运营 18/18、发布校准 33/33；后端定向 27/27、4 个前端脚本 `node --check` 通过；控制台、页面异常、请求失败、4xx/5xx 和横向溢出均为 0。
- 截图证据：`产出\验真\ui_sync_home_after2`、`产出\验真\ui_sync_douyin_after`、`产出\验真\ui_sync_publish_after`，文件均非空可打开。
- 下一阶段：进入云端部署入口与账号工程；当前仍未接入公网、未实现账号隔离，也未进行真实手机物理设备验收。

## 2026-08-28 最新项目决定

- 用户已明确授权修改前端，并要求自行查缺补漏；此前“UI 暂停”决定失效。
- 项目024是重点建设的线上产品，不再以安装包、甲方演示或验收版定义项目；当前本机版本只作为研发与真实能力验真环境。
- 正式方向为“一套能力内核 + 两种体验模式 + 一个隐藏的运营/交付层”：面向年轻创作者的“创作陪跑”，以及面向年长用户、小微企业和非技术用户的“安心交付”。两种模式共用数据、接口、证据边界和审核规则。
- 自动化是交付主路径。公开链接自动采集与分析保持不变；账号数据优先自动连接，CSV/XLSX 只作为连接失败时的故障兜底。
- 目标形态是部署到服务器后通过浏览器持续使用。按量、套餐或额度制尚待真实成本与用户使用数据验证，不能在计量和支付未实现时虚构价格。
- 情绪价值必须来自陪伴、清晰进度、保留用户修改和具体反馈，不使用爆款承诺、虚假积分或过度游戏化。
- 用户默认只看到“粘贴链接 → 看懂 → 写出来 → 拍出来 → 发布前检查 → 发布后复盘”；技术枚举、模型诊断和内部路径继续留在运营/诊断层。
- 任何可能产生费用的内容生成或 Agent 调用，后续产品要求必须有明确的用户确认动作，不能只靠提示文字或隐式确认。
- 手机和线上访问仍受账号体系、租户隔离、访问控制和真实设备验收约束；当前回环监听状态不能被描述为已经上线。

## 当前阶段验收门槛

- UI 修改必须复用现有能力与交互，不因视觉改造破坏采集、分析、账号和发布路径。
- 不把付费内容或 Agent 调用包装成无费用操作；先明确费用确认和零付费边界。
- 不恢复手机入口，直到实现访问控制并完成真实手机允许/拒绝验收。
- 真实作品列表样本已完成核对；用户当前没有更多粉丝样本，该覆盖项延后到自有账号积累或服务器阶段，不再阻塞前端与线上架构准备。
- 不清理旧 v0.2 入口或交付目录，直到用户单独确认归档方案。

## 最终目标

建设一个可持续运营的线上“自媒体通关搭档”。用户通过浏览器粘贴公开链接或连接内容账号，服务端负责媒体获取、字幕提取或转写、画面与数据证据分析、内容生成、发布前检查和发布后复盘，并按明确额度管理高成本任务。

## 已确认约束

- 当前本机环境只承担研发和真实能力验真；线上账号、计费、多租户和生产部署尚未实现，必须如实分层报告。
- 手机端长期目标仍保留，但本轮不以手机访问作为交付阻塞。
- TikTok 访问依赖电脑上的 VPN/系统代理；程序只检测和使用代理，不启动、修改或读取代理客户端私有配置。
- 主路径不能要求普通用户手动粘贴字幕、整理账号表格或理解技术配置。
- 获取顺序为：平台字幕优先；没有字幕时下载音轨并自动转写；分析完成态视频时再做本机代表帧提取、候选镜头结构估算和 RapidOCR 画面文字识别。OCR 只说明画面写了什么，不能替代多模态画面语义理解。
- 优先使用本机 RTX 4060 Ti 和本地 `faster-whisper`；本轮不调用付费 ASR API。
- 界面结论优先、通俗易懂；数字和专业证据默认折叠，通过紧邻数据的“查看依据”入口打开。
- 账号本地档案和本机浏览器助手继续作为自动连接的研发基线；线上环境必须使用官方 OAuth 或经过安全审计的用户侧连接器，不能让服务器读取用户个人浏览器配置。文件导入只保留为故障兜底。
- 项目只有一个活动版本。历史代码快照只能放在活动项目之外，不能形成第二个启动入口。

## 本轮实测基线

- 项目已按用户明确授权在根目录初始化本地 Git，远程目标为公开仓库 `gulaiyugu-collab/24zimeiti`；仓库只纳入当前应用源码、测试、入口和文档，排除凭据、模型、虚拟环境、缓存、运行任务、真实媒体、素材与生成产物。Git 建库前的代码快照与 SHA-256 仍位于 `G:\Workspace\_全局工作台\备份\项目024_字幕流水线前_20260811`。
- 本轮修改前最小回滚基线与 SHA-256 位于 `G:\Workspace\_全局工作台\备份\项目024_公开采集回退前_20260812`，覆盖采集服务、状态机、前端、测试和状态文件。
- 当前 Windows 用户代理 `127.0.0.1:7890` 已实测监听；故障代理 `127.0.0.1:9` 的独立任务返回 `needs_input`、明确提示开启电脑 VPN，且不进入缓存。
- `yt-dlp 2026.7.4`、`ffmpeg`、`ffprobe`、`faster-whisper 1.2.1` 已位于项目运行链路；模型和 pip 缓存均位于项目 G 盘目录。
- GPU 为 NVIDIA GeForce RTX 4060 Ti，显存 8188 MiB。项目虚拟环境已安装 Windows CUDA 12 `cuBLAS/cuDNN` 运行库；服务只修改当前进程 DLL 搜索路径，不修改系统 PATH。
- 未登记真实链接 `6898699405898059010` 在隔离 Worker 中完成媒体、音轨和字幕采集。最终任务 `acq_20260810T175240Z_8718423049b3` 使用 `cuda/int8_float16`，生成 62 字符、2 段带时间码字幕；模型加载 3.962 秒、转写 3.373 秒、总计 7.335 秒，无 CPU 回退。
- 同进程真实样本冷启动总计 7.880 秒；热启动模型加载 0.000 秒、总计 3.576 秒。
- 最终精简清单为 5,030 字节，`raw_artifacts_included=false`；5 个白名单产物均登记大小和 SHA-256，元数据已移除 `cookies`、`formats`、`http_headers` 和签名媒体 URL。
- 同一来源与参数二次提交命中同一 `completed` 任务；`needs_input` 定向测试确认不会进入缓存。
- P0 阶段 Python 回归 39/39 通过；`compileall` 和 `pip check` 通过。仅有 Starlette 对旧 `httpx` TestClient 适配层的弃用警告，不阻止本轮交付。
- 新增 `POST /api/acquisition/jobs/{job_id}/analyze`：非完成态、空字幕、无字幕哈希和客户端字幕覆盖会在内容生成前被拒绝；登记样本复用预先审阅报告。
- Web 无字幕主路径已接成“提交采集 → 轮询终态 → 按任务分析”；人工字幕和媒体上传只作为降级入口。
- 当前 Python 全量回归 44/44、`compileall` 和 `pip check` 通过；桌面与 390px 手机扩展 Playwright 验收 175/175，控制台、页面异常、失败请求、4xx/5xx、横向溢出、裁切和非浮动控件重叠均为 0。
- 真实任务 5 个公开数字与登记样本 4 个公开数字均提供默认折叠的紧邻依据；甲方展开后仅显示通俗的资料来源和采集时间，任务号、接口路径、文件哈希与模型调用元数据只保留在接口 `diagnostics` 中。
- 登记样本已真实展示完整推荐稿、6 段拍摄表和发布配套；当 `publishable=false` 时，第 4 关显示“待补充”，通关保持 3/4，复制动作仍是“复制研究稿”。
- P1-02 最终证据位于 `产出\验真\p1_02_verified_20260811_r11`；该次验收的 `app.js` SHA-256 为 `F3D0B2F788AC2C8A2D06A44460118FBDA5F7871794C9A84D55290495F13B3870`，浏览器脚本 SHA-256 为 `54A7B59FD1822FB3CC87AD443E40CBDC721BC8B6FD5B8D173250A5F88FD51035`。
- 根目录甲方入口已精确路由到当前 `工作文件\app`；桌面快捷方式已改为启动该入口；电脑端服务只监听 `127.0.0.1:8792`。
- 从桌面快捷方式冷启动后获取健康版本 0.3.0；重复启动复用同一服务 PID，不创建第二个服务。
- 电脑专用 Playwright 验收 91/91，证据位于 `产出\验真\p2_desktop_acceptance_20260811_r2`；控制台、页面异常、失败请求、异常响应、溢出、裁切和控件重叠均为 0。
- 真实 TikTok 任务使用 `cuda/int8_float16` 完成本地字幕转写；电脑实时链路与登记样本的结果、证据折叠、完整稿、拍摄表和发布前审核均通过。
- 按用户最新决定，电脑演示入口默认启用内容 Provider；健康接口当前为 `paid_content_enabled=true`，页面自动请求 `analysis_mode=full`。需要停用付费调用时显式使用 `-DisablePaidContent`。
- 抖音真实短链 `https://v.douyin.com/o0Ow5GQ_9BA/` 已由隔离 Worker 完成公开元数据、2,291,362 字节媒体、16 kHz 音轨和本地字幕采集；最终任务 `acq_20260810T212817Z_cc9225c27116` 对应作品 `7670900617237286186`，媒体为 35.015 秒、H.264 1440×1080 + AAC。
- 抖音真实 Worker 使用 `large-v3-turbo`、`cuda/int8_float16` 生成 238 字符、5 段带时间码字幕；5 个白名单产物的文件大小和 SHA-256 与清单逐项一致，Worker stdout/stderr 均为空。
- 抖音元数据已收敛为公开字段和标准作品 URL `https://www.douyin.com/video/7670900617237286186`；最终任务与 `r2` 浏览器证据均未保留 Provider 完整响应、媒体签名 URL、`share_sign`、`did/iid`、Cookie 或请求头。
- 抖音电脑端 Playwright 验收 92/92，证据位于 `产出\验真\p2_douyin_acceptance_20260811_r2`；控制台、页面、失败请求、异常响应、溢出、裁切和控件重叠均为 0，自动分析保持 `paid_api_called=false`。
- 已删除抖音与 TikTok 采集器中的视频时长上限及对应 `needs_input` 分支；单元测试用 1621.9 秒模拟媒体验证两条链路均不再按时长拒绝。为防止异常链接耗尽磁盘，单文件下载安全上限从 250 MiB 提高到 512 MiB，并在超限消息中明确显示数值。
- 原失败短链 `https://v.douyin.com/n0JnmW4HmME/` 已真实完成任务 `acq_20260810T221919Z_a63251b7b033`：作品 `7671561224026344713`，媒体 1621.967 秒、264,153,694 字节、H.264 1920×1080 + AAC；本地 `large-v3-turbo` 使用 `cuda/int8_float16` 生成 10,851 字符、1,185 段字幕。
- 长视频 5 个白名单产物的大小与 SHA-256 全部匹配，自动分析保持 `paid_api_called=false`；电脑 Playwright 92/92，控制台、页面、请求、响应和布局诊断均为 0，证据位于 `产出\验真\p2_douyin_long_video_acceptance_20260811_r1`。
- 当前 Python 全量回归为 50/50，`compileall`、JavaScript 语法检查和 `pip check` 通过。
- 付费完整内容路径已真实调用 `deepseek-chat`；最终页面验收请求 `analysis_mode=full`，使用 8,536 tokens，返回 267 字完整研究稿、10 段拍摄表和发布内容包。浏览器检查 17/17，控制台、页面、请求和异常响应均为 0，证据位于 `产出\验真\p2_paid_full_acceptance_20260811_r2`。
- 页面与当前项目文档的用户可见术语已统一为“发布前审核”；内部 `risk_gate` 字段仅为 API 兼容字段。
- 截图短链 `https://v.douyin.com/vgp9oxHDfmQ/` 经公开重定向解析为作品 `7670900617237286186`，最终作品页 HTTP 200；公共 Provider 的作品号接口 HTTP 200，但视频数据接口 HTTP 400，故障在 Provider 取媒体阶段，不在用户链接。
- 新增稳定作品号解析与跨短链完成缓存；截图短链不强刷时命中完成任务 `acq_20260811T183937Z_db43f4ceb207`，`cache_hit=true`。
- 新增无个人登录态的隔离浏览器回退：公共 Provider 失败后使用临时 Playwright Chromium/Chrome 上下文取得公开视频，再由现有安全下载器落盘；临时媒体地址不写入元数据、清单或 Worker 日志。
- 截图完整分享文案强制刷新任务 `acq_20260811T183937Z_db43f4ceb207` 已真实完成：`public_browser_media_download`、媒体 1,675,042 字节、ffprobe 35.015 秒、本机 `large-v3-turbo` `cuda/int8_float16` 生成 236 字符、3 段字幕；5 个白名单产物大小与 SHA-256 全部匹配，Worker stdout/stderr 均为空。
- 本轮 Python 回归 53/53、`compileall`、`pip check`、应用 JavaScript 语法检查和 PowerShell 语法检查通过；浏览器验收 `p2_douyin_resilience_20260812` 为全通过，控制台、页面、请求诊断均为 0。
- 新截图作品 `7672257862206377216` 使用抖音分离音视频传输。原失败任务 `acq_20260811T191002Z_76ada6348abb` 下载的 41,701,827 字节 MP4 只有 H.264 视频流，没有音频流；因此旧链路在提取 WAV 时返回 `Output file does not contain any stream`，尚未进入字幕转写或内容解读。
- 隔离浏览器现按 `media-video` 与 `media-audio` 响应区分目标流，并使用 `Content-Range` 总长度选取完整候选；临时签名地址仍只存在于 Worker 内存，不进入元数据、清单或日志。
- 失败短链可从本机先前已取得的公开元数据恢复稳定作品号；请求记录同时保留 `submitted_url` 与标准作品 URL。当前截图短链已过期并只跳转抖音首页，本机已根据原失败任务登记其作品号 `7672257862206377216`。
- 强制刷新任务 `acq_20260811T193824Z_7db32edfd251` 已完成：合并媒体 32,476,674 字节、255.485 秒，包含 H.264 1280×720 视频与 AAC 44.1 kHz 双声道音频；本机 `large-v3-turbo` 使用 `cuda/int8_float16` 生成 1,652 字符、149 段字幕。
- 该任务 5 个白名单产物的文件大小和 SHA-256 全部匹配；真实 `DeepSeek deepseek-chat` 完整解读耗时 20.38 秒，`generation_status=completed_research_draft`、`paid_api_called=true`，生成 9 行拍摄表并按证据边界保持 `publishable=false`。
- Python 回归 55/55、`compileall`、`pip check`、应用 JavaScript、浏览器探测脚本和 PowerShell 语法检查通过；浏览器验收 `p2_douyin_split_av_20260812` 全通过，完成缓存、报告渲染、终态停止轮询及控制台、页面、请求诊断均通过。
- 新增结构化 `product_relevance`，统一返回 `has_product`、`no_product` 或 `needs_confirmation`，并附置信度、判断依据、原因、后续动作和用户确认覆盖；规则初判可由完整模型结果补充或修正，用户已提交的商品资料和明确确认优先。
- 资料缺口现分为 `blocking_for_interpretation`、`optional_enhancements` 和 `product_for_rewrite_or_publish`。商品名称、核心卖点、规格和证明材料不再阻塞普通内容解读；无商品内容不返回或展示这些商品缺失项。
- DeepSeek 已完成快速或完整解读后会清除“经模型或人工完成的内容蒸馏”缺失项；无商品内容的拍摄表不再包含 `product_proof`，内容蒸馏改用通用“内容演示”字段。
- 页面新增独立商品属性栏、判断依据、后续意见和待确认按钮；无商品或待确认时隐藏商品资料输入，证据与转写详情默认折叠。桌面与 390px 浏览器验收 26/26，正式 `127.0.0.1:8792` 登记样本验收 11/11，控制台、页面、请求、异常响应和横向溢出均为 0；证据位于 `产出\验真\p2_product_relevance_20260812_r4` 与 `产出\验真\p2_product_relevance_formal_20260812_r4`。
- 本轮最终 Python 全量回归 65/65、`compileall`、JavaScript 语法检查和 `pip check` 通过；正式服务已重启并保持 `version=0.3.0`、`paid_content_enabled=true`。
- P2-08 将 `generation` 与 `acquisition` 从甲方 `report`/`source` 分离到接口 `diagnostics`；前端取消“补充分析”兜底区，未知英文键默认不渲染，数字依据不再展示任务号、接口、哈希或源文件链接。Python 65/65、桌面与 390px 商品属性浏览器 27/27、真实采集缓存与登记样本浏览器 94/94、正式 8792 登记样本 13/13 均通过，控制台、请求、响应、布局和技术字段禁显检查为 0 失败。
- P3-01 已从会全量重写的 JSONL MVP 收口为 SQLite WAL + 显式事务；当前状态与不可更新/删除的追加事件分离，旧 `ledger.jsonl` 只读迁移且不覆盖源文件。核心登记只要求标题和平台，7 维自评与 T+72h 指标区间改为可选复盘基线；无基线也可完成发布、回填和复盘，有基线时只比较预测与实测交集。定向测试 18/18，发布页浏览器验收 33/33。
- P3-02 已接入 FFmpeg 本机代表帧提取与候选镜头结构估算：最多分析前 1200 秒、最多 12 帧、512 MiB 输入上限、90 秒总截止时间，缓存按来源哈希与配置版本校验；OCR 明确返回 `unavailable`。真实已有采集任务生成 12 张可打开 JPEG，主结果页桌面与 390px 浏览器验收 13/13。
- P3-03 已新增抖音运营板块：抖音分析结果可保存为服务端 SQLite 选题，完全相同快照按 SHA-256 去重；每条选题生成 `dy_...` 内容编号，可更新待整理、脚本草稿和可进实验状态，并携带同一编号进入发布实验。Python 全量回归 105/105；抖音主路径浏览器 18/18、主页回归 26/26、发布校准回归 30/30，桌面与 390px 的控制台、页面、网络和布局诊断均为 0 失败。证据位于 `产出\验真\p3_douyin_operations_20260824_r1`。
- 当前零付费 Python 全量回归 99/99；主页浏览器回归 26/26，P3 发布页 30/30，画面结果页 13/13；已知仅有 Starlette TestClient 适配层弃用警告。
- P3-04 本地 OCR 原子单元已验真：RapidOCR 3.9.2 与 ONNX Runtime 1.28.0 位于 G 盘独立 `.venv-ocr`，不需要账号、API Key 或付费；环境实际占用 281.35 MiB。真实抖音视频 12 帧首次完整分析 16.272 秒、识别 154 个去重文字块，二次 0.019 秒并命中 completed 缓存；人工核对 6 个时间点，错误字保留置信度。零付费浏览器 14/14、Python 108/108、`compileall`、3 个 JavaScript `node --check` 和两个环境的 `pip check` 均通过。正式 8792 已从唯一入口重启并返回 `rapidocr_local` completed；证据位于 `产出\验真\p3_04_local_ocr_20260825`。
- P3-04 全文与本地画面语义已验真：27 分钟任务的 HTTP 口播全文 10,851 字与 `transcript.json` 完全一致，共 1,185 段；本机 Ollama `qwen2.5vl:3b` 免费模型位于 G 盘，真实 322 秒抖音任务 4/4 精选帧成功，首次 24.774 秒、二次缓存 0.095 秒，返回 17 条直接观察和 3 条独立推断。四个对应帧均为可打开 JPEG；复杂画中画的位置描述存在偏差并保留限制。零付费 8794 与正式 8792 浏览器均为 16/16，Python 117/117、编译、13 个 JavaScript、3 个 PowerShell 与两个 Python 环境依赖检查通过；证据位于 `产出\验真\p3_04_local_vision_20260825`。

## P3-05 本轮实现与证据（2026-08-26）

- 完整口播入口改为读取一次 `transcript-text` 并显示一整段连续正文；不再渲染逐段时间码、“继续加载”或内部滚动条。真实 322 秒完成任务显示 1,987 / 1,987 字，展示与复制共用同一次请求；OCR 和视觉时间线仍保留时间证据。
- 发布校准明确区分“内容实验”和“复盘基线”：实验记录本轮只改什么，复盘基线用于防止看到结果后倒推解释。7 维自评和指标区间默认折叠且可空，不再冒充播放量预测。
- 新增 `DouyinAccountStore`：账号档案与标准化指标保存到现有抖音运营 SQLite/WAL；创作者中心 CSV/XLSX 是账号数据的优先入口，支持常见作品、留存、包装和粉丝字段，完全相同数据按 SHA-256 去重。新增本机浏览器导出助手，支持 Chrome/Edge/Brave/Chromium/Firefox 路径探测；二维码登录成功后会持续轮询并自动点击导出；原始文件、密码、Cookie、浏览器会话和 OAuth token 均不落盘。
- 账号诊断只把导入数据分成该账号自身前后相邻两段，比较单条播放、5 秒完播、2 秒跳出、封面点击、平均播放、互动和净增粉；建议均附具体变化依据，不使用行业均值或因果承诺。
- 内容分析、抖音运营和发布校准三页共用 `agent-panel.js` 小窗。用户主动点“发送并执行”才会进行一次服务端模型调用并显示 API 用量边界；Agent 先判断是否为修改请求，产生有效改稿后自动写回当前页面，脚本或策略仍可继续编辑。不会自动发布、提交或接管账号。本轮未调用付费模型，只用 `httpx.MockTransport` 验证请求、JSON 和用量门禁。
- Python 全套 135/135、`compileall`、17 个 JavaScript `node --check`、主环境 `pip check` 和新路由契约通过。浏览器验收：账号路径 21/21、Agent 执行 11/11（含等待期间人工修改不被覆盖）、抖音运营 18/18、发布校准 33/33、主页 26/26、真实完整内容 23/23、正式 8792 冒烟 18/18；桌面与 390px 均无控制台错误、页面异常、失败请求或横向溢出。
- 独立只读审查发现并修复四项边界：单行导入不再误报趋势完成；快速切换账号时旧诊断响应不能覆盖新账号或 Agent 写回目标；等待 Agent 响应期间的人工改稿不会被旧响应覆盖；`run.ps1` 在尚无访问控制时拒绝非回环监听，避免局域网设备读取账号数据或触发付费 Agent。相关定向验收已包含在账号路径 21/21 与 Agent 执行 11/11，正式入口已从唯一脚本重启并通过 16/16 只读冒烟。
- 证据位于 `产出\验真\p3_05_account_agent_20260826`。本轮新增真实文件 `C:\Users\Administrator\Downloads\作品列表导出.xlsx` 验真：4 条作品、16 列、无汇总行；`发布时间` 映射为 `date`，账号诊断使用 `date_ascending`。首次导入 `dyi_20260827T163244Z_a9ec638d` 返回 `completed`，相同文件再次导入返回 `deduplicated=true`；数据库 `PRAGMA integrity_check=ok`，P3-05 的真实作品列表样本单元已完成。
- 本轮修复 `工作文件\app\app\services\douyin_accounts.py` 的日期列别名并新增回归测试；`tests.test_douyin_accounts` 8/8、`compileall` 与 `git diff --check` 通过。真实粉丝数据、不同导出版本和官方 OAuth 仍未验真。
- 官方资料确认真实 OAuth 需要网站/移动应用上线、HTTPS `redirect_uri`、所需 `scope` 审核、授权码回调和 token 换取/刷新；入驻文档当前将网站/移动应用列在企业身份范围，个人主体仅支持小游戏和小玩法。本轮没有开放平台应用资质，页面如实显示“官方授权筹备中”，没有伪造登录成功；若平台后台规则更新，以当前可选主体和审核结果为准。

## P3-07 重点线上产品入口与双体验模式（2026-08-28）

- 项目定位已从安装包/甲方验收版调整为重点线上 Web 产品；当前 Windows 本机服务只作为研发与真实能力验真环境。新增 `项目文档\产品定位与线上化路线.md`，明确线上用户路径、自动化原则、账号连接、商业模式验证和 P4 上线前置。
- 首页新增“创作陪跑 / 安心交付”分段选择，默认安心交付；选择保存在当前浏览器。两种模式只改变首屏语言、辅助提示和主动作，共用分析接口、结果、证据和发布审核。
- 抖音账号弹窗调整为自动连接主路径，CSV/XLSX 收入“自动连接失败时”兜底区；当前本机连接器与未来官方 OAuth/用户侧安全连接器的边界已在页面和设计文档中说明。
- 零付费隔离服务验收：首页 32/32，账号与 Agent 路径 24/24；完整 Python 回归 136/136、`compileall`、6 个 JavaScript `node --check` 与 `git diff --check` 通过。测试中预期的 Provider 失败堆栈来自失败路径 fixture，不是回归失败；仅有既有 Starlette TestClient 弃用警告。
- 正式 `127.0.0.1:8792` 只读冒烟 21/21：双模式、产品工作台标识、自动连接优先、文件兜底、浏览器能力、官方 OAuth 未连接边界、Agent 展开和发布页均通过；控制台、页面、请求、4xx/5xx 和横向溢出均为 0，Agent `call_count=0`，未产生本轮付费调用。
- 截图与结果位于 `产出\验真\p3_07_online_product_ui_20260828`。

## P2-02 旧版本归档（2026-08-28）

- 已将 `产出\\项目024_自媒体通关搭档_甲方验收版_v0.2` 只移动、不删除至 `产出\\归档\\项目024_自媒体通关搭档_甲方验收版_v0.2`。
- 移动前目录为普通目录，不是 Junction/SymbolicLink；移动前 20 个文件、676288 字节，移动后目标存在、原位置不存在，入口脚本和验收证据均随目录保留。
- 当前活动项目仍只有一个本地兼容 `.cmd` 入口；归档目录不作为第二个可运行版本。

## 空链接按钮反馈修复（2026-08-28）

- 用户截图中的绿色按钮是首页 `#analyzeButton`。点击时 URL 输入为空，原逻辑只在输入框下方写入 `#urlError`，没有同步显示表单级提示，因此用户容易感觉“没有反馈”；此时本来就不会发起分析网络请求。
- 已修复 `工作文件\\app\\static\\app.js`：空链接或格式错误时同时调用 `showFormMessage(...)`，保留输入框红色校验和焦点。
- 正式 `127.0.0.1:8792` 真实浏览器复测：按钮点击后 `urlError=请先粘贴一个公开内容链接。`、表单提示可见且包含“还没有链接”，控制台错误为 0。
- `browser_v03.cjs` 已加入空链接反馈回归断言；JavaScript 语法检查和 Python 全量回归 136/136 通过。

## 前端静态资源缓存修复（2026-08-28）

- 用户反馈模式按钮“按下动一下后没有反应”。现场截图中的页面标题是新 HTML，但主按钮仍为旧文案“生成完整脚本”，证明浏览器复用了旧 `app.js`；旧脚本没有双模式点击监听。
- 已为首页、抖音运营页和发布页的 CSS、页面脚本及 Agent 脚本增加版本查询参数 `?v=20260828-2`，强制浏览器重新加载当前资源，不改变业务接口。
- 正式 `127.0.0.1:8792` 真实浏览器复测：实际请求均带 `v=20260828-2`；模式点击后标题、主按钮和 `aria-checked` 正确切换；空链接同时显示输入错误和表单级反馈；控制台错误为 0。

## 当前原子任务

当前原子任务已切换为“重点线上产品入口改造”。先按以下顺序推进：

1. P3-07 双体验模式与自动连接主路径已完成。
2. 按 P4-01 至 P4-05 推进线上架构、账号隔离、用量计量、自动账号连接和生产部署。

更多粉丝数据与导出版本延后到自有账号积累或服务器阶段；P2-02 归档、P2-03 真实手机、P4 线上化、公开评论和主页多作品采集仍保留在后续队列。

## 已完成原子项

- `P0-01` 持久进度与回滚基线：`feature_list.json` 可解析；项目记忆读取器已加载项目024的 `CORE.md`、`STATE.md`、`INDEX.md` 且未截断；活动项目外备份包含 64 条 SHA-256 记录。
- `P0-02` TikTok 代理预检与公开媒体采集：真实成功链路、真实代理阻塞、平台字幕优先、音轨兜底、元数据白名单和隔离 Worker 已验真。
- `P0-03` GPU 本地字幕转写：项目 G 盘模型与 CUDA 运行库、RTX 4060 Ti 实际推理、带时间码字幕、冷启动和热启动耗时已验真。
- `P0-04` 精简证据清单与缓存：清单长度边界、白名单产物、SHA-256、默认上下文隔离、完成缓存和非完成不缓存已验真。
- `P1-01` 采集结果自动进入内容分析：完成态清单、运行时字幕、数字指标和证据哈希已自动进入同一分析任务；Web 主路径无需手动字幕，登记样本与不足证据边界均保留。
- `P1-02` 通俗移动端结果与折叠证据：桌面和 390px 手机均验真真实任务与登记样本；数字依据逐项折叠，完整稿、拍摄表和发布核对使用普通用户语言，未通过发布前审核时不伪报完成。
- `P2-01` 电脑后端与电脑浏览器真实验收：桌面快捷方式冷启动、真实 TikTok 采集与分析、登记样本完整产出和 91/91 浏览器检查均已通过；付费模式默认值随后按用户新决定调整。
- `P2-04` 抖音公开单链接采集：真实短链完成媒体、GPU 字幕、精简证据清单、付费完整内容生成和电脑端验收，未使用个人登录态。
- `P2-04` 长视频补充验收：已移除视频时长上限，27 分钟真实抖音视频完成媒体、GPU 字幕、自动分析和电脑端 92/92 验收。
- `P2-05` 抖音采集抗故障与持续验收：公共 Provider 失败时自动切换隔离浏览器；短链按稳定作品号复用完成缓存；状态提示不再把 Provider 故障归咎于链接；真实截图分享文案、媒体、GPU 字幕、清单和电脑页面路径均已验真。
- `P2-06` 抖音分离音视频与失败恢复：隔离浏览器识别并合并独立画面/声音流；失败任务保留短链到作品号恢复线索；原失败作品已完成 GPU 字幕、DeepSeek 研究稿和电脑页面验收。
- `P2-07` 商品属性分流与资料分层：商品、非商品和模糊内容均有可解释判定；商品资料只进入改写或发布范围，普通解读不受阻；模型完成后旧蒸馏缺口会清除，前端独立栏和确认交互已在桌面、390px 及正式入口验真。
- `P2-08` 甲方结果页技术元数据收敛：后端诊断字段与用户报告分层；前端取消未知字段兜底展开，数字依据只保留资料来源和采集时间；桌面、390px、真实缓存采集和登记样本均完成反向禁显验收。
- `P3-01` 发布校准闭环：登记内容实验→发布登记→同窗口回填→复盘；7 维自评与指标区间是可选复盘基线，无基线不计算偏差或经验候选。SQLite WAL 保存当前状态，不可变追加事件保留历史；18 个定向测试与发布页 33/33 浏览器验收通过。样本不足 3 条时不声称形成稳定规律。
- `P3-02` 代表帧与候选镜头结构：对采集清单内已校验视频执行本机 FFmpeg 分析，展示真实代表帧、候选切点、片段数与启发式节奏；输入路径不由客户端指定，结果不泄漏本机路径/来源哈希，最多 1200 秒、12 帧、512 MiB、总计 90 秒。OCR 未安装并如实标注；19 个相关 Python 测试与画面页 13/13 浏览器验收通过。
- `P3-04` 本地 OCR：新增可注入 `LocalOCRProvider` 与独立 UTF-8 Worker，Provider 名称、版本和配置进入缓存键；completed 缓存严格校验帧文件、文字块、坐标、置信度和帧引用。真实 12 帧 OCR、二次缓存、正式 8792 与桌面/390px 页面均已验真。
- `P3-04` 本地视觉与全文：新增 loopback-only `LocalOllamaVisionProvider`，每次最多均匀选择 4 帧，使用固定无数组结构并过滤 OCR 回声、否定项、重复项与推断；观察和推断分别带帧、时间、置信度、Provider 与可打开 URL。完整口播、OCR、视觉时间线和桌面/390px 页面已验真。
- `P3-05` 本地实现：账号档案、CSV/XLSX 标准化导入、账号自身趋势诊断、三页 Agent 小窗、脚本/策略写回与费用确认门禁均已通过 fixture、Mock 和浏览器验收；真实作品列表文件已验真，真实自动连接与官方 OAuth 尚未验真。

## 云端 Worker 运行时前置（2026-08-29）

- 新增 `工作文件/app/app/services/cloud_worker_runner.py`：独立出站 Worker 客户端，支持云端任务领取、租约心跳、完成/失败回传、空闲轮询和可停止运行。
- Worker 默认只把任务 payload 映射为现有 `AcquisitionJobRequest`，复用本地采集、ASR 和缓存内核；不开放入站端口，不读取或上传 Cookie、浏览器 profile 或本地密钥。
- HTTP 客户端支持可选 `Authorization: Bearer` 头，但当前合约仍以测试用 `X-Worker-Id` 为身份标识；正式部署必须替换为托管身份验证/JWT，不能直接把测试标头暴露到公网。
- 新增 `tests/test_cloud_worker_runner.py`，覆盖空闲、成功、失败、非法 payload、配置边界、停止运行和 HTTP 客户端标头；定向测试 `7/7` 通过。
- 本轮完整 Python 回归 `152/152` 通过；Worker CLI `--help` 和现有 3 个前端脚本 `node --check` 通过。
- 该运行时尚未接入主应用或真实云端控制面；没有云端账号、部署 URL 或预算授权，因此没有上传代码、创建资源或产生外部费用。

## 云端账号隔离与托管数据库准备（2026-08-29）

- 修正 `CloudTaskStore` 幂等约束：`idempotency_key` 改为按 `owner_id + idempotency_key` 唯一，避免不同用户使用相同键时互相去重。
- 新增隔离回归：协议和 HTTP 合约均验证同键跨用户会生成不同任务；定向云端协议/HTTP/Runner 测试合计 `19/19` 通过。
- 新增 `工作文件/app/deploy/supabase/schema.sql`：Supabase Postgres 的 `cloud_tasks`、`usage_events`、`invites` 表，任务用户级 RLS、用量记录和邀请表服务端专用边界；未连接真实 Supabase，SQL 仅完成静态结构检查。
- 新增 `工作文件/app/deploy/README.md`：面向小白的后续人工入口和密钥边界说明。
- 本轮完整 Python 回归 `155/155` 通过；`git diff --check` 通过；本机未创建云端资源、未上传代码、未设置或输出密钥。

## Supabase 项目初始化（2026-08-29）

- 用户已创建 Supabase Free 项目，Project ID：`lkfjdebtontzwddbsyth`，区域为 Southeast Asia（Singapore）。
- 用户已在 SQL Editor 成功执行 `deploy/supabase/schema.sql`；Table Editor 已显示 `cloud_tasks`、`usage_events`、`invites` 三张表。
- 本轮使用无密钥请求现场检查 `https://lkfjdebtontzwddbsyth.supabase.co/rest/v1/` 与 `/auth/v1/health`，均返回 HTTP `401`；这证明项目地址可达且服务要求认证，不代表应用已完成认证接入。
- 用户未提供或公开任何 API Key、数据库密码或 service-role 凭据；当前仍未把本地代码部署到公网。
- 新增 `工作文件/app/.env.example`，只包含 Supabase、控制面和 Worker 的占位变量；真实值只应填写在部署平台环境变量中。

## 验收标准

## 手机注册阻断修复（2026-08-31）

- 公网现场复核：`/api/health`、`/api/cloud/config` 和 `/cloud` 均返回 `200`，但页面引用的 `/static/cloud.js` 返回 `404`；因此注册与登录按钮没有加载事件处理代码，点击无反应，不是用户输入问题。
- 控制面已增加 `/static/cloud.js` 的明确只读路由，并对手机页和脚本返回 `Cache-Control: no-store`，避免修复后仍复用旧页面。
- 注册接口的无效输入现场请求返回 `{"detail":"请输入有效邮箱"}`，证明注册后端路由可达；成功注册的公网用户路径仍需在服务器重新构建后验收。
- 定向测试 `4/4` 通过，覆盖页面、脚本路由、内容类型、缓存头以及国内注册/登录/任务闭环。
- 当前服务器只能判定为“控制面进程健康但部署不完整”：公网仍直接暴露 HTTP `8787`，`80/443` 未形成可访问入口；本机 Worker 在线、SQLite 持久化挂载、备份恢复、监控告警和真实手机完整任务均未在本轮验真。
- 手机入口刷新后任务消失的原因已确认：旧脚本只保存登录会话，没有保存 `task_id`；新增任务 ID 持久化、刷新自动恢复轮询和失效任务清理。云端 Worker 同时增加启动、领取、完成/失败和轮询重试提示，避免常驻进程静默退出。
- 上述任务状态与 Worker 可观测性修复已提交并推送到 `main`（当前最新提交 `585bbf6`），服务器需要再次运行 `工作文件/app/deploy/update-control-plane.sh` 才会生效；旧页面创建的任务记录仍保留在云端数据库，但旧页面不会自动恢复其 ID。
- 本机 Worker 首次启动失败的现场原因已确认：`PROJECT024_CLOUD_WORKER_ID` 使用中文计算机名，`httpx` 在构造 HTTP header 时触发 `UnicodeEncodeError`，请求尚未发出。Worker 现将主机名转换为 ASCII 安全 ID，并在启动/断线/领取/完成时输出可见状态；定向测试 `13/13` 通过。最新代码提交为 `585bbf6`。

## 国内分析上线准备（2026-08-31）

- 国内正式路线已切换为腾讯云服务器自包含控制面：本地 SQLite 保存账号和任务，手机登录与任务查询不再依赖 Supabase；原 Supabase 模块仅保留兼容，不进入国内 Docker 启动路径。
- 手机入口 `/cloud` 已接入国内注册、登录、提交公开链接、任务轮询和结果展示；本机 Worker 继续复用采集、ASR、OCR 与本地视觉内核。
- 国内模式注册→登录→提交→Worker 领取→完成→用户查询闭环测试通过；全量 Python 回归 `168/168` 通过，前端脚本语法检查通过。
- Git 提交 `6027146` 已通过 SSH 推送到远端 `main`。腾讯云服务器尚未购买或创建，公网域名、HTTPS 和真实手机访问仍未验收；当前没有产生云资源费用。
- 本阶段上线范围锁定为链接采集、字幕/转写、OCR/画面识别和分析报告，不包含自动配音、自动剪辑或自动发布。
- Worker 已补齐“采集完成后调用现有分析内核”的步骤，手机任务结果不再停留在证据清单；付费内容模型是否启用仍由服务器是否配置对应密钥决定。

1. 当前代码从明确的电脑端启动入口运行，并监听 `127.0.0.1:8792`；健康检查与页面资源对应当前文件哈希。
2. 电脑浏览器真实 TikTok 链路、任务轮询、结果与证据入口均可用。
3. 电脑浏览器控制台、请求、核心按钮、溢出和遮挡均无未解释错误。
4. 默认电脑演示调用已配置的付费内容 API；生成结果仍保持研究稿和发布前审核边界。
5. 抖音真实短链自动完成媒体、字幕、清单和分析，标准作品 URL 与证据文件不保留签名查询参数。
6. 公共 Provider 暂时失效时，已确认的公开作品仍能通过隔离浏览器回退取得媒体；回退不可用时返回通道故障边界，不伪造内容。
7. 抖音使用分离音视频流时，最终媒体必须同时包含视频流和音频流；只取得画面时不得进入 ASR 或伪报解读完成。
8. 每条内容必须返回可解释的商品属性状态；待确认不阻塞普通解读，并允许用户明确确认。
9. 无商品内容不得显示商品名称、核心卖点、规格、商品证明或商品拍摄证明缺失项。
10. 已登记视频应展示真实代表帧、明确标为“机器估算”的候选镜头结构、带时间点/坐标/置信度的本地 OCR，以及可回到具体帧的本地多模态观察；OCR、观察与推断不得互相冒充。
11. 普通用户页面不得出现“补充信息”、Token、`json_object`、采集任务号、内部 API 路径、原始技术枚举或 64 位文件哈希。

## 当前风险

- TikTok 页面、签名和媒体地址可能变化，必须保留可替换的采集适配层。
- 抖音当前默认依赖社区公共 Provider `douyin.wtf`，不是官方 SLA 接口；已提供 `PROJECT024_DOUYIN_PUBLIC_API_BASE` 替换边界，Provider 变化、限流或失效仍会影响实时采集。
- 抖音回退依赖本机 Node.js、`playwright-core` 和可启动的 Chrome/Edge；入口已做依赖预检。抖音页面结构或 CDN 响应变化仍可能要求更新探测器。
- 回退链路已真实验证 35 秒合并流作品与 255 秒分离音视频作品；分段媒体、不同地区页面及更多编码组合仍需继续抽样，超过 512 MiB 仍会安全阻断。
- 视频时长不设应用层上限；为避免异常链接耗尽本机磁盘，单媒体文件仍保留 512 MiB 下载安全上限，超过时页面会明确提示。
- Windows CUDA 运行库约占项目虚拟环境 2.08 GB；不能改成依赖未登记的系统 Python DLL。
- 本地 OCR 最多覆盖 12 张代表帧，本地多模态模型最多分析其中 4 张精选帧，均不能宣称逐帧完整；复杂分屏、细小物体和人物位置可能误判，必须通过对应帧人工复核。
- 公开评论采集仍未接入，现作为可选增强项展示，不阻塞普通内容解读。
- 付费内容生成已经真实验收；只有判断为商品内容且进入商品改写或正式发布时，商品事实才参与发布前审核。画面、来源或风险证据不完整时仍保持研究稿，不能冒充可直接发布的成稿。
- 手机端真实访问从延期项改为当前 MVP 主路径，必须用真实设备完成登录、提交链接、查看结果和反馈验收；不等同于已具备大规模公网访问控制。
- 用户最新拍板：先做手机端云端最小闭环，小范围邀请朋友内测，先看是否有真实市场需求和可改进点；朋友只获得使用权，不交付源码或完整可转售副本。
- 安装包是后续商业化方向，不是当前第一阶段；后续保留 Windows 安装包、本地优先/云端兜底混合版和无电脑云端版三种交付形态，共用一套能力内核。
- 当前电脑演示会默认调用 DeepSeek 并产生费用；用户可通过 `-DisablePaidContent` 显式切换为零付费模式。
- Agent 小窗每次发送会调用一次已配置内容模型并产生 API 用量；本轮未做真实付费 Agent 调用，实际输出质量仍需用户在界面中单次确认。
- 创作者中心导出格式可能随账号和页面变化；当前 CSV/XLSX 映射已完成 fixture 和一份真实作品列表验真，但粉丝数据与更多导出版本未覆盖，不能声称账号数据回流完全闭环。
- 官方 OAuth 受开放平台应用上线、HTTPS 回调、scope 审核和账号授权阻塞；当前入驻文档将网站/移动应用列在企业身份范围，个人主体不能把小程序或 App 当作绕过主体审核的路径。
- v0.2 历史交付目录已归档到 `产出\\归档`，不作为活动版本或用户入口。
- 外部备份可回滚代码，但不是第二个可运行版本。
- 归档已完成且未删除源内容；后续若要清理归档目录，仍需单独确认。

## 精确下一步

下一步进入外部授权边界：选择并授权一个云端控制面（账号、部署入口和预算上限），再把现有 HTTP 合约替换为真实 JWT/Worker 凭据、接入托管数据库/RLS，并完成 HTTPS、双账号隔离和真实手机路径验收。在获得授权前，继续只做本地协议、运行时和合约测试，不把项目描述成已上线。

## 新对话续跑

新对话必须先读取：

1. `G:\Workspace\AGENTS.md`
2. 本文件
3. `G:\Workspace\Projects\项目024_自媒体通关搭档\feature_list.json`
4. `G:\Workspace\Projects\项目024_自媒体通关搭档\项目文档\项目概览.md`

随后读取 `项目文档\产品定位与线上化路线.md`，再按本文件的“当前活动范围”“当前阶段验收门槛”和“精确下一步”续跑。前端修改已获授权；服务器、支付、账号权限、OAuth 和旧目录移动仍需按各自安全边界单独验真或确认。
