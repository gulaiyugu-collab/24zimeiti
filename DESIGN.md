---
name: 自媒体通关搭档线上产品工作台
references:
  - Intercom quiet product-led workspace
  - Shopify transactional clarity for small businesses
---

# 产品设计方向

项目024是重点建设的线上自媒体工作台，不是甲方验收页、安装包展示页或一次性脚本工具。当前 Windows 本机版本是研发和真实能力验真的运行环境，产品目标是部署到服务器后由用户通过浏览器持续使用，并支持按量、套餐或额度制等商业方式。

界面不是营销落地页。第一屏直接进入真实工作：选择适合自己的使用方式，粘贴公开链接，系统自动完成获取、理解、原创、拍摄建议、发布前检查和发布后复盘。

# 一套内核，两种体验

- `创作陪跑`：面向希望获得灵感、反馈和持续创作节奏的用户。语言更有陪伴感，强调“今天做什么”和“下一条怎么更好”。
- `安心交付`：面向年长用户、小微企业和非技术用户。语言直接、步骤明确、默认隐藏专业选项，强调“交给系统处理”和“现在只需做什么”。
- 两种模式共用数据、接口、分析结果、证据边界和发布审核，不复制业务逻辑。
- 模式只改变文案、默认展开项和操作节奏，不改变事实结论或风险判断。
- 默认进入 `安心交付`；用户选择保存在当前浏览器，随时可切换。

# 自动化原则

自动化是正式产品主路径，不能把手工导入设计成交付前提：

1. 公开作品使用链接自动采集、字幕获取或转写、画面分析和内容生成。
2. 账号数据优先自动连接和同步。
3. 当前本机浏览器助手是自动连接的研发实现；线上部署后需替换或扩展为官方 OAuth 或用户侧安全连接器。
4. CSV/XLSX 只保留在“连接失败时使用”的故障兜底区域，不作为默认入口。
5. 自动连接失败必须说明失败层级和下一步，不伪造已登录、已同步或官方授权成功。

# 信息架构

1. 顶部保持单一产品导航：内容工作台、抖音运营、发布复盘。
2. 首页输入区顶部提供紧凑的分段模式选择，不使用长篇产品说明。
3. 链接输入是唯一首要动作；字幕、媒体和商品事实继续按需展开。
4. 结果沿“看懂 → 原创稿 → 拍摄 → 发布前检查 → 复盘”推进。
5. 抖音运营页的账号入口改为“连接账号数据”，自动连接优先，文件导入降级。
6. 内部诊断、Provider、任务号、路径、哈希、Token 和运营配置不进入普通用户界面。

# 线上产品边界

- 未部署前不在界面宣称“云端已上线”“自动续费”或“官方账号已连接”。
- 商业模型保持可配置：按量、套餐和额度制进入后续验证，不在尚无计费系统时展示虚假价格。
- 上线前必须完成账号体系、租户数据隔离、任务队列、并发和配额、费用确认、支付幂等、HTTPS、审计、备份恢复和隐私协议。
- 付费模型调用必须有明确确认、额度显示和停止条件；不得仅靠提示文字视为同意。

# 视觉系统

- 延续当前浅灰画布、白色工作表面、深灰文字和克制绿色强调色。
- 参考 Intercom 的安静产品层级与 Shopify 面向商家的清晰操作关系，不复刻品牌外观。
- 控件圆角 6px，主要面板不超过 10px；不使用渐变、玻璃拟态、装饰性大图或营销 Hero。
- 工具界面保持紧凑但不拥挤；桌面适合扫描和重复操作，390px 下保持完整点击区域。
- 字距固定为 0，动态内容不得造成布局位移或横向溢出。

# 本轮验收

- 首页真实显示并可切换“创作陪跑 / 安心交付”，刷新后保留选择。
- 两种模式的标题、主按钮和辅助提示随选择变化，分析接口和结果结构保持一致。
- 用户可见区域不再使用“甲方演示版”作为产品定位，版本号不抢占品牌位置。
- 抖音账号弹窗以自动连接为首要动作，CSV/XLSX 收入故障兜底。
- 桌面和 390px 无控制台错误、页面异常、失败请求、横向溢出或控件遮挡。
